export { launchMap } from './launch';
export { rocketMap } from './rocket';

/*TODO: for these models the "item" key only applies to object arrays.
 This needs to be handled for instances where a single object is returned */